HTTPS Redirect provides a simple mechanism to switch a URL from HTTP to HTTPS (SSL) based on the document-type (alias), node id or template alias.

This package will add the following appSetting keys to your Web.config file: "HttpsRedirect:DocTypes", "HttpsRedirect:PageIds" and "HttpsRedirect:Templates"